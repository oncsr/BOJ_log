#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);

    char a;
    while(cin>>a){
        if(a == 'w')    {cout<<"chunbae";return 0;}
        if(a == 'b')    {cout<<"nabi";return 0;}
        if(a == 'g')    {cout<<"yeongcheol";return 0;}
    }
}